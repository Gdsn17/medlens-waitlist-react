apiVersion: v1
kind: Service
metadata:
  name: medlens-frontend-service
  namespace: medlens-waitlist
  labels:
    app: medlens-frontend
spec:
  selector:
    app: medlens-frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP
